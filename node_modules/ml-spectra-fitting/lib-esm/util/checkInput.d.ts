import { DataXY, DoubleArray } from 'cheminfo-types';
import { sumOfGaussians } from '../shapes/sumOfGaussians';
import { OptimizeOptions, Peak1D } from '../spectra-fitting';
/** Algorithm to check the input
 * @param data - Data to check
 * @param peakList - List of peaks
 * @param options - Options for optimization
 */
export declare function checkInput(data: DataXY<DoubleArray>, peakList: Peak1D[], options: OptimizeOptions): {
    y: number[];
    x: DoubleArray;
    maxY: number;
    minY: number;
    peaks: Peak1D[];
    paramsFunc: typeof sumOfGaussians;
    optimization: import("../spectra-fitting").OptimizationOptions;
};
//# sourceMappingURL=checkInput.d.ts.map