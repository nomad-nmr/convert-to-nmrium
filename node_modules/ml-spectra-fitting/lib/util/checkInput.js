"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.checkInput = void 0;
const ml_array_max_1 = __importDefault(require("ml-array-max"));
const sumOfGaussians_1 = require("../shapes/sumOfGaussians");
const sumOfLorentzians_1 = require("../shapes/sumOfLorentzians");
const sumOfPseudoVoigts_1 = require("../shapes/sumOfPseudoVoigts");
const assignDeep_1 = require("./assignDeep");
let xObject = {
    init: (peak) => peak.x,
    max: (peak) => peak.x + peak.fwhm * 2,
    min: (peak) => peak.x - peak.fwhm * 2,
    gradientDifference: (peak) => peak.fwhm * 2e-3,
};
let yObject = {
    init: (peak) => peak.y,
    max: () => 1.5,
    min: () => 0,
    gradientDifference: () => 1e-3,
};
let fwhmObject = {
    init: (peak) => peak.fwhm,
    max: (peak) => peak.fwhm * 4,
    min: (peak) => peak.fwhm * 0.25,
    gradientDifference: (peak) => peak.fwhm * 2e-3,
};
let muObject = {
    init: (peak) => peak.shape && peak.shape.mu !== undefined
        ? peak.shape.mu
        : 0.5,
    min: () => 0,
    max: () => 1,
    gradientDifference: () => 0.01,
};
/** Algorithm to check the input
 * @param data - Data to check
 * @param peakList - List of peaks
 * @param options - Options for optimization
 */
function checkInput(data, peakList, options) {
    let { shape = { kind: 'gaussian' }, optimization = {
        kind: 'lm',
    }, } = options;
    let peaks = JSON.parse(JSON.stringify(peakList));
    let kind = shape.kind.toLowerCase().replace(/[^a-z]/g, '');
    let paramsFunc;
    let defaultParameters;
    switch (kind) {
        case 'gaussian':
            paramsFunc = sumOfGaussians_1.sumOfGaussians;
            defaultParameters = {
                x: xObject,
                y: yObject,
                fwhm: fwhmObject,
            };
            break;
        case 'lorentzian':
            paramsFunc = sumOfLorentzians_1.sumOfLorentzians;
            defaultParameters = {
                x: xObject,
                y: yObject,
                fwhm: fwhmObject,
            };
            break;
        case 'pseudovoigt':
            paramsFunc = sumOfPseudoVoigts_1.sumOfPseudoVoigts;
            defaultParameters = {
                x: xObject,
                y: yObject,
                fwhm: fwhmObject,
                mu: muObject,
            };
            break;
        default:
            throw new Error('kind of shape is not supported');
    }
    let x = data.x;
    let maxY = (0, ml_array_max_1.default)(data.y);
    let y = new Array(x.length);
    let minY = Number.MAX_VALUE;
    // we need to move the data down to the baseline 0
    for (let i = 0; i < x.length; i++) {
        y[i] = data.y[i];
        if (data.y[i] < minY) {
            minY = data.y[i];
        }
    }
    // removing minY from each y, dividing by max
    for (let i = 0; i < x.length; i++) {
        y[i] = (y[i] - minY) / maxY;
    }
    peaks.forEach((peak) => {
        peak.y /= maxY;
        peak.shape = {
            kind: shape.kind,
            ...peak.shape,
        };
    });
    let parameters = (0, assignDeep_1.assignDeep)({}, defaultParameters, optimization.parameters);
    for (let key in parameters) {
        for (let par in parameters[key]) {
            if (!Array.isArray(parameters[key][par])) {
                parameters[key][par] = [parameters[key][par]];
            }
            if (![peaks.length, 1].includes(parameters[key][par].length)) {
                throw new Error(`The length of ${key}-${par} is not correct`);
            }
            for (let index = 0; index < parameters[key][par].length; index++) {
                if (typeof parameters[key][par][index] === 'number') {
                    let value = parameters[key][par][index];
                    parameters[key][par][index] = () => value;
                }
            }
        }
    }
    optimization.parameters = parameters;
    return {
        y,
        x,
        maxY,
        minY,
        peaks,
        paramsFunc,
        optimization,
    };
}
exports.checkInput = checkInput;
//# sourceMappingURL=checkInput.js.map