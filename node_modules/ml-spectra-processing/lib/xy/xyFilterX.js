"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.xyFilterX = void 0;
const getZones_1 = __importDefault(require("../zones/utils/getZones"));
/** Filter an array x/y based on various criteria x points are expected to be sorted
 *
 * @param points - points
 * @param options - options
 * @return filtered array
 */
function xyFilterX(points, options = {}) {
    const { x, y } = points;
    const { from = x[0], to = x[x.length - 1], exclusions = [] } = options;
    let zones = (0, getZones_1.default)(from, to, exclusions);
    let currentZoneIndex = 0;
    let newX = [];
    let newY = [];
    let position = 0;
    while (position < x.length) {
        if (x[position] <= zones[currentZoneIndex].to &&
            x[position] >= zones[currentZoneIndex].from) {
            newX.push(x[position]);
            newY.push(y[position]);
        }
        else {
            if (x[position] > zones[currentZoneIndex].to) {
                currentZoneIndex++;
                if (!zones[currentZoneIndex])
                    break;
            }
        }
        position++;
    }
    return {
        x: newX,
        y: newY,
    };
}
exports.xyFilterX = xyFilterX;
//# sourceMappingURL=xyFilterX.js.map