import { SparseMatrix } from 'ml-sparse-matrix';
export default function getPauliMatrix(mult: number): {
    x: SparseMatrix;
    y: SparseMatrix;
    z: SparseMatrix;
    m: SparseMatrix;
    p: SparseMatrix;
};
